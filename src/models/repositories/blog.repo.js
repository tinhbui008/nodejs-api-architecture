"use strict";

const { blog } = require("../../models/blog.model");

const findAllDraftForCreatedBy = async ({ query, limit, skip }) => {
  console.log("findAllDraftForCreatedBy");
};

const createBlog = async ({ query, limit, skip }) => {
  console.log("findAllDraftForCreatedBy");
};

module.exports = {
  findAllDraftForCreatedBy,
};
